# 系统章程

## 定位

`charter/` 是系统约束的根。`model/` 应在章程约束下建立，用可执行模型表达和验证已经明确的章程内容；模型本身不反向定义章程。

## 当前阶段

当前暂时从已有 `model/` 开始，先建立一一对应的章程目录与文件骨架，便于后续逐步补写约束并校准模型。这是阶段性的引导方式，不改变 charter 对 model 的约束关系。

除本页的总体定位外，大部分章程内容暂不编写。各占位页中的模型链接仅用于当前阶段的映射和追踪，不表示模型是章程的约束来源。

临时模型映射：[model/main.spec](../model/main.spec)
