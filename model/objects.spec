/* Kernel execution objects. */

spec cpu;
spec kernel_image;
spec scheduler;
spec task;
spec boot_stack;
spec printk;
spec dtb_blob;
spec memblock;
spec memory_node;
spec mem_map;
spec zone;
spec early_console;
spec user_app_runtime;
spec vm;
